Para el funcionamiento del programa se nos facilito una estructura en la cual se declaraban en primer lugar todas las variables que serían útiles para la realización del mismo.
En el siguiente bloque de código, se solicitaban loss datos que según el problema descrito se necesitaban.
En el siguiente bloque de código pasabamos a convertir las respuestas que fueran necesarias a datos numéricos para poder pasar a operarlas.
En el siguiente bloque comparamos los datos en los rangos de edad para proceder a la realización de las operaciones que nosa arrojarian los resultados necesarios para hacer los recargos extras.
Pasamos a sumar los costos del seguro para dar el total final al usuario.

En sí en el problema habia que considerar si tenia o no esposa, si tenia o no hijos y ya en esta parte extra, habia que considerar si tenia o no propiedades y agregar un recargo por su salario.
estas consideraciones las hicimos con if, para comparar las respuestas del usuario y con el .toUpperCase() verificabamos la respuesta, aunque eel usuario hubiese ingresado mayúsculas o minúsculas.
lo que se facilitaba con este código.

Por mi experiencia creo que es mas entendible ir corroborando paso a paso cada dato, digamos porque en ocasiones para buscar algun error nos perdemos un poco, pero es cuestión de cada persona, 
logré integrarme al formato que se presentó, para agregar datos fue más fácil, ya que en una parte eran solo variables y en otra solo prompt, y así... aunque ir haciendo las variables conforme las voy necesitando
se me hace un poco mas entendible, lo que se podría mejorar a la final sería esto, para la comprensión del mismo. Otro tema podría ser la validación de datos extra ya que hay que tomar en cuenta un mal tecleo de
parte del usuario, lo que llevaría a ingresar datos erroneos, más en las palabras, quizá en los nombres, pero como no tengo el conocimiento de como hacerlo no sabría decir si se haria más largo el código o más complejo.
